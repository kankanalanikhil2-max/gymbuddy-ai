version: 1.0
runtime: nodejs22
build:
  commands:
    build:
      - npm install
      - npm run build
  env:
    - name: NODE_OPTIONS
      value: "--max-old-space-size=2048"
    - name: CI
      value: "true"
run:
  command: npm start
  network:
    port: 3000
