# AWS App Runner configuration (required when "Use a configuration file" is selected)
# Uses Node.js 20 managed runtime. Build and run from source; no Dockerfile needed for this path.
version: 1.0
runtime: nodejs20
build:
  commands:
    build:
      - npm ci
      - npm run build
run:
  command: npm start
  network:
    port: 3000
