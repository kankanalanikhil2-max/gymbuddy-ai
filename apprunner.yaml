# AWS App Runner configuration (required when "Use a configuration file" is selected)
# Uses Node.js 22 (latest supported App Runner runtime).
version: 1.0
runtime: nodejs22
build:
  commands:
    build:
      - npm ci
      - npm run build
run:
  command: npm start
  network:
    port: 3000
